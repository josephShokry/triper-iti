import { DetailsPipePipe } from './details-pipe-pipe';

describe('DetailsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
